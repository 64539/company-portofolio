I will refactor the website to achieve the high-end agency aesthetic and functionality you requested.

### **1. Setup & Dependencies**

* **Action**: Install `resend` for email functionality.

* **File**: `package.json`

### **2. Fundamental Design: The Neural Nerve (Background)**

* **Action**: Replace the current canvas-based `NeuralBackground` with a static SVG-based system.

* **Details**:

  * Create a single fixed SVG layer.

  * Draw an ultra-thin (1.5px) organic path (Bezier curves) that visually "snakes" from the Hero section down to the Contact section.

  * Apply a linear gradient stroke (`#00f2fe` to `#7000ff`).

  * Add a glowing effect using `filter: drop-shadow(0 0 8px #7000ff)`.

* **File**: `src/components/ui/NeuralBackground.tsx`

### **3. Content & Data Updates**

* **Action**: Update `TEAM_MEMBERS` and `SERVICES` (modal details) to match your specifications.

* **Details**:

  * **Team**: Update list to 8 members (Ryuzacki, Jabriel, Rizqy, Hadi, Tobing, Yudis, Farhan, Aji) with correct roles.

  * **Services**: Update `flow` to 3 steps (Discovery -> Synthesis -> Engineering) and `leadExpert` format ("Software by CTO Office").

* **Files**: `src/constants/index.ts`, `src/constants/services-detail.ts`

### **4. "The Collective" (C-Suite Hierarchy)**

* **Action**: Refactor the grid layout and mobile view.

* **Details**:

  * **Desktop**: Highlight CEO (Ryuzacki) and CTO (Jabriel) as dominant cards.

  * **Mobile**: Stack members in a vertical list with `aspect-[1/1.1]` and ensure no text overflow (`truncate` or proper wrapping).

* **File**: `src/components/sections/Collective.tsx`

### **5. "High-Impact Solutions" (Services) Refactor**

* **Action**: Fix overflow issues and implement the new grid.

* **Details**:

  * Remove fixed heights/overflow constraints.

  * Switch to a responsive grid: 1 column (mobile) -> 4 columns (desktop).

  * Apply `text-wrap: balance` to descriptions.

  * Ensure optical alignment ($\phi \approx 1.618$).

* **File**: `src/components/sections/Services.tsx`

### **6. Service Modal System**

* **Action**: Enhance the modal to be a "Deep Dive" experience.

* **Details**:

  * **Header**: Title + Neon Icon.

  * **Layout**: 2-column layout (Expertise + 3-step Tech Flow).

  * **Lead**: Display "Software by CTO Office" etc.

  * **Animation**: 0.3s spring scale-up entry using `framer-motion`.

* **File**: `src/components/ui/ServiceModal.tsx`

### **7. Logic & Navigation (The 2-Second Sync)**

* **Action**: Implement the "Sync with Collective" interaction.

* **Details**:

  * **Hero Button**: Rename to "Sync with Collective".

  * **Interaction**: On click -> Show top-bar neon loading bar (2s) -> Smooth scroll to `#collective` -> Trigger visual pulse on the Neural Nerve.

  * **"The Lab" Button**: Direct smooth scroll to `#capabilities`.

  * **Typography**: Apply `text-[clamp(2.5rem, 8vw, 5rem)]` to Hero heading.

* **File**: `src/components/sections/Hero.tsx`

### **8. Functional Integration (Contact)**

* **Action**: Enable real email sending via Resend.

* **Details**:

  * Create API route `src/app/api/contact/route.ts` to handle form submissions.

  * Wire up the contact form to this API.

  * **Success State**: Change button to "Pesan Terdisintesis ✓" with a glowing cyan border.

* **Files**: `src/components/sections/Contact.tsx`, `src/app/api/contact/route.ts`

### **9. Mobile Polish & Safety**

* **Action**: Apply global spacing and safety fixes.

* **Details**:

  * Add global `px-6` padding for mobile containers.

  * Verify all transitions use `ease-in-out`.

* **File**: `src/app/globals.css`

- **Trae, implement a simple global state (using** **`Zustand`** **or** **`React Context`) to trigger the** **`isSyncing`** **state. This will ensure the Loading Bar in the Navbar component reacts instantly when the Hero button is clicked.**
- **For the Neural Nerve SVG, use** **`framer-motion`** **with** **`pathLength`,** **`pathSpacing`, and** **`pathOffset`. Create a repeating 'pulse' animation that travels along the stroke using** **`animate={{ pathOffset: [0, 1] }}`** **with a duration of 3-5 seconds.**

<br />

**Trae, ensure the Resend API Key is pulled from** **`.env.local`. Create a** **`.env.example`** **file so I know which keys to fill in later.**

