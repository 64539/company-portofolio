I will refactor the website to implement the C-Suite hierarchy, Neural Flow background, and Services Modal system as requested.

### **Phase 1: 'The Collective' (C-Suite Hierarchy) & Mobile Fixes**

1. **Data Update (`src/constants/index.ts`):**

   * Update `TEAM_MEMBERS` with the specific 8 founders and their new roles:

     * **Ryuzacki (CEO)**, **Jabriel (CTO)**, **Rizqy (CPO)**, **Hadi (CMO)**, **Tobing (COO)**, **Yudis (CFO)**, **Farhan (Sales)**, **Aji (Chief of Staff)**.

   * Assign specific "Core Node" status to CEO/CTO.

2. **Component Refactor (`src/components/sections/Collective.tsx`):**

   * **Staggered Neural Layout:** Implement a CSS Grid layout where CEO/CTO cards are larger (`col-span-2` vs `col-span-1` or similar).

   * **Neural Connection:** Add `framer-motion` SVG lines that appear on hover, connecting the hovered card to the CEO/CTO (or all others).

   * **Mobile View:** Convert the grid to a clean, scrollable vertical list with consistent aspect ratios (`aspect-[1/1.1]`).

### **Phase 2: 'Neural Flow' Background (Aura Style)**

1. **New Component (`src/components/ui/NeuralBackground.tsx`):**

   * Create a **Mesh Gradient Background** using CSS/Canvas where two gradient orbs (Cyan & Purple) move and intersect.

   * The "Meeting Line" (high contrast area) will serve as the visual guide.

2. **Layout Integration (`src/app/layout.tsx`** **&** **`globals.css`):**

   * Remove the static SVG Axon Line.

   * Inject `NeuralBackground` into the main layout to span the entire page.

   * Ensure the background is fixed/sticky so content scrolls over it.

### **Phase 3: 'Our Services' Modal Transformation**

1. **New Component (`src/components/ui/ServiceModal.tsx`):**

   * Create a `framer-motion` based modal that scales up from the center.

   * **Content:**

     * **Domain Overview:** Deep-dive text.

     * **Expertise & Tech Flow:** 3-step process visualization.

     * **Lead Office:** Dynamic mapping (e.g., "Software" → "CTO Office").

     * **Tech Stack:** Monochrome icons grid.

2. **Interaction Update (`src/components/sections/Services.tsx`):**

   * Remove `Link` routing to `/services/[slug]`.

   * Implement state to track the active service and trigger the modal on click.

### **Phase 4: Mobile & Symmetry Polish**

1. **Hero Typography (`src/components/sections/Hero.tsx`):**

   * Apply `text-[clamp(2.5rem, 10vw, 5rem)]` and `text-wrap: balance` to the Heading.

   * Ensure "SYNERGIZIN" fits without overflow.
2. **Global Safety:**

   * Verify `px-6` padding on all mobile containers.

   * Ensure the Neural Aura line remains centered and visible on mobile.

Trae, ensure a strict Z-index hierarchy: Background (`z-0`), Main Content (`z-10`), Navbar (`z-50`), and Service Modal (`z-[100]`). Ensure the `NeuralBackground` uses `pointer-events-none` so it doesn't block clicks on buttons/links. For the `NeuralBackground`, use `will-change: transform` or `canvas` if the CSS blur approach causes frame drops. Keep the animation subtle (long duration, 20s+) to ensure low CPU usage while maintaining a fluid 'aura' look. When the `ServiceModal` is active, apply a `backdrop-filter: brightness(0.3)` to the overlay to 'dim' the Neural Background, making the modal content the absolute focal point. Ensure the modal itself has a `backdrop-blur-xl` to keep the glassmorphism vibe.

Contoh Struktur Data untuk Phase 3:

const SERVICE\_DETAILS = {
software: {
title: "Neural Software Architecture",
lead: "CTO Office (Jabriel)",
flow: \["Logic Mapping", "Scalable Synthesis", "Neural Deployment"],
stack: \["Next.js", "Rust", "PostgreSQL"],
description: "Building resilient digital backbones that adapt to high-traffic neural loads."
},

product: {
title: "Product Experience Synthesis",
lead: "CPO Office (Rizqy)",
flow: \["User Behavior Mining", "Prototyping", "Emotional Logic Design"],
stack: \["Figma", "Spline", "Linear"],
description: "Designing interfaces that feel like a natural extension of human thought."
}

// ...dan seterusnya buat Marketing, Sales, dll.
}
