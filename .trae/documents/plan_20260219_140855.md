I will implement the Admin Gate and requested updates as follows:

### 1. Backend & Data Storage (Upstash Redis)

* **Install Dependencies**: Add `@upstash/redis` and `date-fns`.

* **Redis Client**: Initialize the Redis client in `src/lib/redis.ts`.

* **Contact API (`api/contact/route.ts`)**: Update to store form submissions in a Redis List (`contact:messages`) in addition to sending emails.

  * Data structure: `{ id, name, email, whatsapp, subject, message, status, createdAt }`.

* **Admin APIs**:

  * `api/admin/auth/route.ts`: Simple password verification.

  * `api/admin/messages/route.ts`: Endpoints to **GET** (fetch messages), **PATCH** (update status), and **DELETE** (remove messages).

### 2. Frontend - Admin Gate & Panel

* **Navbar Trigger (`Navbar.tsx`)**:

  * Implement a "Multi-Click" detector on the Logo (7 clicks in 3 seconds).

  * Trigger the Admin Modal upon success.

* **Admin Modal (`components/admin/AdminModal.tsx`)**:

  * **Login View**: Simple password input.

  * **Dashboard View**:

    * **Layout**: Responsive Card Grid (Mobile-first).

    * **Card Content**: Name, Time, Service, Status, Message snippet.

    * **Actions**:

      * **Read**: Expand full message.

      * **Reply**: `mailto:` link with the requested subject/body format.

      * **Chat WA**: `https://wa.me/...` link if number exists.

      * **Delete**: Remove from Redis.

    * **Status Updates**: Clicking 'Reply' or 'Mark Done' updates the status badge immediately.

### 3. Contact Form & Footer Updates

* **Contact Form (`Contact.tsx`)**:

  * Add "WhatsApp Number (Optional)" field.

  * Validate as numeric/tel.

  * Pass this data to the API.

* **Footer (`Footer.tsx`)**:

  * Update Social Links with the specific URLs provided (WhatsApp, Instagram, Gmail).

  * Implement "Futuristic Hover" (Cyan/Neon glow) for icons.

  * Ensure stack/grid layout on mobile.

* **Constants**: Update `src/constants/index.ts` with the new URLs to ensure consistency across the app.

### 4. Verification

* Verify the 7-click trigger works.

* Verify data is stored in Redis upon submission.

* Verify Admin Panel actions (Reply/Chat/Delete) function correctly.

Trae, sedikit tambahan untuk rencana kamu:

1. **Auth Session:** Simpan status admin di `localStorage` atau `sessionStorage` dengan masa berlaku singkat agar tidak perlu klik logo berulang kali jika halaman ter-refresh.
2. **Haptic/Visual Feedback:** Berikan efek getar singkat (`navigator.vibrate`) atau kilatan cahaya (glow effect) pada logo saat klik ke-7 berhasil dideteksi.
3. **WhatsApp Pre-filled Message:** Untuk fitur 'Chat WA', gunakan template pesan otomatis yang menyertakan nama klien dan jenis layanan agar admin tidak perlu mengetik dari nol.
4. **Date Formatting:** Gunakan `date-fns` untuk menampilkan waktu masuk pesan yang manusiawi (contoh: '2 mins ago' atau 'Today at 14:00') agar admin mudah memprioritaskan pesan terbaru.

<br />

**Trae, saya sudah menyiapkan database di Upstash Redis. Sekarang silakan eksekusi rencana kamu dengan detail tambahan berikut:**

1. **Connection:** Gunakan kredensial dari `.env` (`UPSTASH_REDIS_REST_URL` dan `UPSTASH_REDIS_REST_TOKEN`) untuk inisialisasi Redis client di `src/lib/redis.ts`.
2. **Data Model:** Simpan setiap pesan masuk menggunakan perintah `hset` dengan key `message:[id]` dan tambahkan ID tersebut ke dalam list `message_inbox` menggunakan `lpush` agar pesan terbaru selalu ada di urutan teratas.
3. **Admin Verification:** Gunakan `ADMIN_PASSWORD` dari environment variable untuk memvalidasi akses di modal Admin Gate.
4. **API Security:** Pastikan endpoint `api/admin/messages` melakukan pengecekan header atau token sederhana agar tidak bisa di-access sembarangan oleh publik.
5. **Ready for Push:** Jika sudah selesai dan lulus uji coba lokal, beri tahu saya agar saya bisa push ke Vercel dan memasukkan environment variables di dashboard Vercel.

