I have analyzed the current codebase and the user's requirements. Here is the plan to refactor the website layout for optimal responsiveness and structure.

### 1. Global Styles & Spacing (`src/app/globals.css`)

* **Action**: Implement a CSS reset and update global variables.

* **Details**:

  * Update spacing system to use base unit multipliers (8px).

  * Refactor `.section-spacing` to meet the max 80px (mobile) / 120px (desktop) gap requirement.

  * Define custom CSS variables for spacing if needed or stick to Tailwind's scale.

  * Ensure container classes (`.phi-container`) are consistent.

### 2. Navbar Optimization (`src/components/ui/Navbar.tsx`)

* **Action**: Refactor header layout and logo sizing.

* **Details**:

  * **Logo**: Use `w-[1.5em]` (or scaling class) for mobile to desktop ratio. Ensure SVG scales correctly.

  * **Layout**: Use `flex justify-between items-center` with no extra margins.

  * **Spacing**: Remove any dead space on the right.

### 3. Hero Section Refactor (`src/components/sections/Hero.tsx`)

* **Action**: Optimize spacing and animation structure.

* **Details**:

  * Remove unnecessary wrappers.

  * Ensure `h1` and content scale correctly without overflow.

  * Verify animation properties (use `transform`/`opacity` only).

### 4. About Section Grid (`src/components/sections/About.tsx`)

* **Action**: Implement CSS Grid for Mission/Vision cards.

* **Details**:

  * **Mobile**: `grid-cols-1`.

  * **Desktop**: `grid-cols-2` (since there are 2 cards, user asked for min 3 columns for "Mission, Vision, Lab" generally, but About only has Mission/Vision usually. I will use a responsive grid that *can* support more, or stick to the 2-col layout if it fits the content better, ensuring `minmax` is used).

  * **Gap**: Consistent gap using the 8px multiplier system.

### 5. Lab Section Grid (`src/components/sections/Lab.tsx`)

* **Action**: Implement CSS Grid for Lab projects.

* **Details**:

  * Refactor the current manual split (`1.618fr 1fr`) to a more robust CSS Grid using `minmax` where appropriate, or ensure the current grid collapses correctly to 1 column on mobile (already does `grid-cols-1 lg:grid-cols-[...]`, will verify spacing).

  * Ensure `minmax(300px, 1fr)` logic is applied where feasible for the list of small projects.

### 6. Services Section Grid (`src/components/sections/Services.tsx`)

* **Action**: Refactor Services grid.

* **Details**:

  * Current: `grid-cols-1 md:grid-cols-2 lg:grid-cols-4`.

  * Requirement: Min 3 columns desktop. Current is 4, which is fine.

  * Ensure aspect ratios are consistent.

### 7. Collective Section Grid (`src/components/sections/Collective.tsx`)

* **Action**: Refactor Team grid.

* **Details**:

  * Ensure mobile view stacks correctly (already does).

  * Optimize grid gaps.

### 8. Animation & Performance

* **Action**: Review all `framer-motion` usages.

* **Details**:

  * Add `will-change` to animated elements.

  * Ensure `layout` prop usage is performant.

  * Verify `pointer-events: none` during animation (can be tricky with React state, will mostly rely on CSS transitions where possible or simple Framer variants).

### 9. Code Cleanup

* **Action**: Remove empty `div`s and standardize HTML tags.

* **Details**:

  * Replace generic `div` with `section`, `article` where applicable.

  * Remove unused classes.

### 10. Verification

* **Action**: Verify against requirements (breakpoints, spacing, lighthouse targets).

I will start by updating the global styles to set the foundation.

<br />

**Trae, tolong tambahkan detail berikut ke dalam rencana refactor kamu:**

1. **Services Data:** Update `Services.tsx` untuk menampilkan 4 grid: Fullstack Dev, Design, Edit Video, dan Jasa Titip (Jastip).
2. **Popup to Form Logic:** Pada modal 'Initialize Project', saat tombol diklik, arahkan user ke section `#contact`. Implementasikan logika agar form di section contact secara otomatis terisi (auto-fill) pada field 'Layanan/Pesan' sesuai dengan modal yang sedang dibuka.
3. **Lab Section Content:** Update `Lab.tsx`. Untuk proyek pertama, buat detail tentang 'Proses Pembuatan Web Jastip' (tampilkan flow dari discovery hingga deployment). Untuk dua proyek lainnya, buat placeholder menarik bertema 'Neural UI Kits' dan 'Scalable Architecture'.
4. **Grid Consistency:** Pastikan Grid di Services tetap 4 kolom di desktop (lg:grid-cols-4) dan collapse menjadi 1 kolom di mobile dengan gap yang sesuai dengan sistem multiplier 8px kamu.

